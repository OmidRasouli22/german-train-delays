# Which database dbt talks to. The name here must match the profile named
# in dbt_project.yml.
#
# DuckDB keeps everything in one file, so there is no server to run and no
# password to keep out of git.
trains:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ../trains.duckdb
      threads: 4
